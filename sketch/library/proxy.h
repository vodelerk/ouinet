#ifndef OUINET_PROXY_H
#define OUINET_PROXY_H

#include "client.h"

namespace ouinet {

class Proxy
{
	// Proxy(Client* client, int port, ...);
	// start()
	// stop()
	
// 	// runs a http proxy on a TCP port. Runs all incoming requests through client->dispatch()
};

} // namespace ouinet

#endif
