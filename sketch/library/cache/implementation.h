#ifndef OUINET_CACHE_IMPLEMENTATION_H
#define OUINET_CACHE_IMPLEMENTATION_H

namespace ouinet {
namespace cache {

class CacheImplementation
{
	// abstract class
	//
	// retrieve(url)
	// store(url, data)
		// only with private key
	// seed(url, signed-data)
	// unseed(url)
	// availability(url)
};

class Implementation
{
	// abstract class
	//
	// ServiceImplementation cache(publickey)
	// ServiceImplementation cache(privatekey)
};

} // namespace cache
} // namespace ouinet

#endif
